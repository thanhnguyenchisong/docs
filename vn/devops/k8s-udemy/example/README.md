# Example — Kubernetes (Udemy) mẫu

Dùng chung manifest từ [../k8s/example](../k8s/example): `deployment.yaml` (Deployment + Service). Chạy với minikube/kind để thực hành theo tài liệu k8s-udemy.

```bash
kubectl apply -f ../k8s/example/deployment.yaml
```

Đọc kèm [../README.md](../README.md).
