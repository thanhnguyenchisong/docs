{
  "name": "prisma-example",
  "version": "1.0.0",
  "description": "Prisma + Express REST API example (SQLite)",
  "private": true,
  "scripts": {
    "setup": "npx prisma generate && npx prisma migrate dev --name init && npx prisma db seed",
    "dev": "node src/index.js",
    "seed": "node prisma/seed.js",
    "studio": "npx prisma studio"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
